# Local development (works with Vite proxy + local server)
VITE_API_URL=/api/v1

# Optional explicit API origin (used by editor asset URL normalization)
# VITE_API_ORIGIN=http://localhost:3001

# Production example (set in Vercel project env vars)
# VITE_API_URL=https://farols-tech-server.vercel.app/api/v1
